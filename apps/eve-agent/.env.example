AI_GATEWAY_API_KEY=
# GPT-5.6 Luna requires paid Vercel AI Gateway credits.
# Replace this with another Eve-supported gateway model when using the free tier.
EVE_MODEL=openai/gpt-5.6-luna

# Shared only between the Eve deployment and the example Convex deployment.
EVE_AGENT_TOKEN=replace-with-a-long-random-value
